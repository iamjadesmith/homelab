---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: scrypted-claim
  namespace: default
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
