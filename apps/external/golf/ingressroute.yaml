---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: golf
  namespace: default
  annotations:
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`golf.joejad.com`)
      kind: Rule
      services:
        - name: golf
          port: 8081
      middlewares:
        - name: default-headers
  tls:
    secretName: local-joejad-tls
