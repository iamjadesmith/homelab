---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: longhorn
  namespace: longhorn-system
spec:
  chart:
    spec:
      chart: longhorn
      sourceRef:
        kind: HelmRepository
        name: longhorn
      version: 1.10.1
  interval: 1m0s
  values:
    defaultSettings:
      backupTarget: "nfs://joejadserver.joejad.lan:/home/joejad/backups/k3s"
